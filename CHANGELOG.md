# Atalanta changelog

## v0.0.1

- stacks: Node.js (npm), Node.js (yarn), Ruby, Rust (Cargo)
- `--install` command to install codebase on the local machine
- `--setup` command to prepare the codebase for development
- auto-completion for Fish shell
